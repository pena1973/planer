// ==========================================================

export async function payByStripe(teamId: number, amount: number, token: string) {
  // TODO: интеграция со Stripe
  return { ok: true };
}
